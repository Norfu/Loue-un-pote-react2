import React from 'react';import { View, Switch, Text, Button } from 'react-native';import { useForm, Controller } from 'react-hook-form'; const FormWithSwitch = () => {   const { control, handleSubmit, watch } = useForm({     defaultValues: {       switchValue: false    }   });   const onSubmit = data => console.log(data);   return (     <View><Controllercontrol={control}name="switchValue"render={({ field: { onChange, onBlur, value } }) => (           <SwitchonValueChange={onChange}value={value}onBlur={onBlur}          />        )}      />      <Button title="Submit" onPress={handleSubmit(onSubmit)} /><Text>Switch is {watch('switchValue') ? 'ON' : 'OFF'}</Text></View>  ); }; export default FormWithSwitch;